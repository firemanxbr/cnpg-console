replicaCount: 1
image:
  repository: cnpg-console
  pullPolicy: Never
  tag: dev
dex:
  enabled: true
  connectors: []
  staticClients:
    - id: cnpg-console
      name: CNPG Console
      secret: dev-secret
      redirectURIs: ["http://localhost:8080/callback"]
service:
  type: NodePort
  port: 8080
