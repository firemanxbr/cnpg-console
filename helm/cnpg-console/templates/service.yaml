apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnpg-console.fullname" . }}
  labels: {{- include "cnpg-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector: {{- include "cnpg-console.selectorLabels" . | nindent 4 }}
